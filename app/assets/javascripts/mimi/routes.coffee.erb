class Mimi.Routes
  @connectAccountUrl    = <%= SPREE_AUTH_URL.to_json %>
  @createAccountUrl     = <%= "#{MAD_MIMI_URL}/service_agreements/solo_signup?redirect_url=#{CGI::escape(SPREE_AUTH_URL)}".to_json %>
  @disconnectAccountUrl = <%= disconnect_mad_mimi_path.to_json %>
