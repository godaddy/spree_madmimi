class Mimi.Routes
  @connectAccountPath    = <%= SPREE_AUTH_PATH.to_json %>
  @createAccountUrl     = <%= "#{MAD_MIMI_URL}/service_agreements/solo_signup?redirect_url=%{redirect_url}".to_json %>
  @disconnectAccountUrl = <%= disconnect_mad_mimi_path.to_json %>
